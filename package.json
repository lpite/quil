{
  "private": false,
  "name": "quil",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^8.7.0",
    "eslint-config-preact": "^1.1.3",
    "preact-cli": "3.3.3",
    "sirv-cli": "^2.0.2"
  },
  "dependencies": {
    "preact": "^10.6.4",
    "preact-render-to-string": "^5.1.2",
    "preact-router": "^4.0.1",
    "typescript": "^4.5.5"
  }
}
